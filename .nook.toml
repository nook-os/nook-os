# What this repo tells nook about itself (MAIN-359).
#
# A general settings file that currently has one section. Unknown top-level
# keys are IGNORED rather than rejected, so a future `[build]` or `[sessions]`
# section is additive and an older control plane reading a newer file keeps
# working.
#
# This is a DECLARATION, not a detection: nothing scans the source for
# hardcoded ports. What this file says is what nook believes.

# The ports a session in this repo needs (MAIN-376). The control plane leases a
# free number per entry from the node's range and exports it as `env`, so N
# worktrees of this repo run side by side instead of fighting over one port.
#
# An app in a session binds `$THAT_VARIABLE`, never a literal — the convention
# CLAUDE.md documents, and this file is where it is stated.
#
# WHAT IS DECLARED HERE IS THE DEV STACK'S HOST BINDINGS, and only those. The
# eleven below are every port `docker compose up` publishes to the machine, and
# they are what two checkouts of this repo actually collide on. Ports INSIDE the
# compose network are deliberately absent: services reach each other by service
# name on a fixed container port, and two stacks each get their own network, so
# those never contend. Declaring them would lease numbers nothing reads.
#
# Every `env` below is consumed by `docker-compose.yml` as
# `${VAR:-<previous default>}`, so an unset variable — a plain clone, no nook —
# publishes exactly the ports this repo always did.
#
# `required = true` throughout — the second half of MAIN-359's own note: a repo
# whose session IS its server wants it, so exhaustion fails loudly rather than
# starting an app that then collides on a hardcoded default. This repo's session
# is the stack.
#
# `false` was the first cut here, and it is actively unsafe in a way that looks
# like the kinder setting. Measured on a node with the range narrowed to sixteen
# ports: the second session leased 2 of 11 and STARTED, the third leased 0 and
# STARTED. Compose cannot tell "unset because this is a plain clone" from "unset
# because the node ran out" — both are just an absent variable — so those
# sessions fall back to 5432/5173/8080 and collide on precisely the literals
# this card removed. Silently.
#
# The control plane is the only layer that can tell those two apart, and
# `required` is how it says so. The cost is real and deliberate: EVERY session
# in this repo leases eleven ports, shells and agents included, so a node
# advertising 100 supports nine concurrent sessions and the tenth is refused BY
# NAME — "no free port for `control` (NOOK_CONTROL_PORT): every port in
# 4200-4299 is leased on this node". Widen the node's range to raise that. A
# refusal that names the port is recoverable; a silent collision is not.
#
# IF YOU CHOOSE `false`, check `NOOK_PORTS_UNSATISFIED` (MAIN-377). A session
# that was skipped a listener now carries that variable naming it, comma
# separated, and carries NOTHING when everything was leased — so a startup
# script can tell the two absent-variable cases apart at last:
#
#     [ -n "$NOOK_PORTS_UNSATISFIED" ] && {
#       echo "no port for: $NOOK_PORTS_UNSATISFIED" >&2; exit 1; }
#
# That is what makes `false` a real choice rather than a silent one. It does not
# make it the safe default: an app that ignores the variable still collides.

# ── WHICH SESSIONS NEED THESE (MAIN-378) ────────────────────────────────────
#
# `runtimes` on a listener says which session runtimes need it, and OMITTING the
# key means every runtime. That is the mechanism this repo does NOT use, and the
# absence below is deliberate — this comment is here so nobody reads it as an
# oversight and "fixes" it.
#
# The first cut scoped all eleven to `["bash", "zsh"]`, reasoning that CLAUDE.md
# describes the stack coming up once per checkout and agents then editing
# against containers that rebuild themselves. **The owner's 2026-08-05 ruling
# reversed that: agent sessions — `claude` and every other runtime that can run
# the stack — keep their ports.**
#
# The reason is the failure direction. An agent following the documented
# `docker compose up -d` loop with no variables set falls through every
# `${VAR:-<literal>}` below and binds 8080 / 5432 / 5173 — the shared literals
# MAIN-376 removed — and it is not even told: `NOOK_PORTS_UNSATISFIED` fires for
# a listener that was WANTED and unavailable, never for one this runtime never
# asked for, so a runtime-scoped-out session gets neither the ports nor the
# notice. Silently binding a shared literal is unrecoverable and invisible; an
# exhausted range refuses BY NAME and is recoverable. Safety wins over the
# capacity number.
#
# **And "every runtime that can run the stack" is written as ABSENCE rather than
# a list, because a list goes stale in exactly the dangerous direction.** Every
# runtime here gets a shell and can therefore run `docker compose up -d`;
# spelling out `["bash", "zsh", "claude", "codex", "hermes"]` would mean the
# next runtime somebody adds silently leases nothing and rebinds 8080 — the very
# bug the ruling closed, deferred until nobody is looking for it. There is no
# enumeration to keep current if there is no enumeration.
#
# So the ceiling here is still nine concurrent sessions against a 100-port node,
# for every session type. That is the accepted cost, not an oversight: MAIN-378
# is the MECHANISM, and this repo is not a repo the mechanism helps. A repo whose
# agent sessions genuinely cannot publish anything is where it pays off.

# The control plane: the browser door and the agent (mTLS) door.
[[ports]]
name = "control"
env = "NOOK_CONTROL_PORT"
protocol = "tcp"
required = true

[[ports]]
name = "agent"
env = "NOOK_AGENT_PORT"
protocol = "tcp"
required = true

# The web app (Vite dev server) — the one a person actually opens.
[[ports]]
name = "web"
env = "NOOK_WEB_PORT"
protocol = "tcp"
required = true

# Team chat, its own service.
[[ports]]
name = "chat"
env = "NOOK_CHAT_PORT"
protocol = "tcp"
required = true

# Postgres, published so a psql or a GUI on the machine can reach it. The
# services themselves go over the compose network, not through this.
[[ports]]
name = "postgres"
env = "NOOK_PG_PORT"
protocol = "tcp"
required = true

[[ports]]
name = "redis"
env = "NOOK_REDIS_PORT"
protocol = "tcp"
required = true

# Object storage: the S3 API and its console are separate listeners.
[[ports]]
name = "minio"
env = "NOOK_MINIO_PORT"
protocol = "tcp"
required = true

[[ports]]
name = "minio-console"
env = "NOOK_MINIO_CONSOLE_PORT"
protocol = "tcp"
required = true

# The SQS-compatible queue.
[[ports]]
name = "sqs"
env = "NOOK_SQS_PORT"
protocol = "tcp"
required = true

# Dev mail: SMTP in, web inbox out. Two listeners, one service.
[[ports]]
name = "smtp"
env = "NOOK_SMTP_PORT"
protocol = "tcp"
required = true

[[ports]]
name = "mailpit"
env = "NOOK_MAILPIT_PORT"
protocol = "tcp"
required = true

# What a fresh build worktree gets seeded with (MAIN-481).
#
# The DEFAULT is every ignored file, which is right for the small ones this
# repo actually needs in a worktree — `.env`, and `deploy/dev-certs/`, without
# which the stack boots and dies. It is very wrong for the build caches: on a
# working checkout `target/` is ~9 GB and `.cache/` ~88 GB, and a seed would
# copy them synchronously before the pass could start. `.cache/` is also
# written by containers as root, so a host-side copy of it fails as well as
# being enormous.
#
# `.nook-secrets/` is excluded on principle rather than for size: it holds the
# fleet's Claude session, the node already mounts it where the agent looks, and
# a credential should not be duplicated into every card's worktree.
[worktree]
exclude = [
  "/target",
  "/target-docker",
  "/target-docker-node",
  "/.cache/",
  "node_modules/",
  "dist/",
  ".pnpm-store/",
  ".nook-secrets/",
  "frontend/apps/desktop/src-tauri/target",
  "frontend/apps/desktop/src-tauri/binaries/",
]

# What a concluded build run gives back to the disk (MAIN-493).
#
# Every path here is the compiler's output and nothing else: delete it and the
# next pass rebuilds exactly what it needs. Cargo names an artifact for its unit
# CONFIGURATION rather than its content, so each differently-configured build —
# clippy, the Postgres test leg, the SQLite leg, host and container — adds a
# whole artifact set and removes none. One build worktree's `target/` reached
# 120 GB and filled the machine on 2026-08-09. `docs/build-artifact-growth.md`
# has the measurements.
#
# The two `.cache/cargo-target*` entries are the CONTAINERS' target dirs
# (`docker-compose.yml` bind-mounts them at `/app/target-docker*`) and are the
# biggest of the set. They are written by root inside the container, so a
# host-side delete may only get part of them; that is reported, not fatal.
#
# `frontend/node_modules` is the pnpm half, and it is here for the same reason
# `target` is: it is LINKED from a store rather than downloaded, so the next
# pass reproduces it locally. Measured on `azul` 2026-08-11 across the twelve
# live worktrees, a tree that ran the HOST-side `pnpm install` carries 218 MB
# here — the largest single directory left standing after MAIN-493's set had
# run. That install relinks from the host's global store
# (`~/.local/share/pnpm/store`, outside every worktree), so the next pass pays
# a local relink rather than a refetch.
#
# The host and container installs are DISJOINT, which is what makes these
# entries safe to delete. A host-installed tree has a real 218 MB
# `frontend/node_modules` and no `frontend/.pnpm-store` at all; the container
# instead installs into `.cache/web-node-modules` (bind-mounted over
# `/app/frontend/node_modules` by `docker-compose.yml`), leaving the host path
# EMPTY — the mount lives in the container's namespace and shadows it, so a
# host-side reclaim never reaches the container's files either way. Measured
# against a live container on 2026-08-11 the host-side removal simply succeeds;
# were a kernel to refuse it instead, the entry would be reported refused and
# still free nothing, so the safety of this list does not rest on which happens.
#
# The five workspace packages are here because `reclaim` matches LITERAL paths
# and `frontend/pnpm-workspace.yaml` declares `packages/*` and `apps/*`, each of
# which gets its own `node_modules`. In the container shape that is where nearly
# all of the host-side pnpm bytes actually are: `build-…-MAIN-138` and
# `build-…-MAIN-524` measure 1 MB at the root and 18 MB nested, so the root
# entry alone would have left the larger half standing.
#
# NOT here, deliberately, and each for a measured reason rather than for
# appearing in `exclude` (MAIN-538 AC-2):
#
#   `.cache/cargo-registry` (872 MB) is a DOWNLOAD cache, refilled from the
#   network rather than by compiling. Reclaim is for what a rebuild reproduces
#   locally.
#
#   `.cache/web-node-modules` (233 MB) is NOT a cache despite the path — it is
#   the web container's live `node_modules`, the install `pnpm install` writes
#   on every `docker compose up`. Deleting it from under a running stack buys
#   233 MB and costs a reinstall the developer did not ask for.
#
#   `frontend/.pnpm-store` (212 MB) is the CONTAINER's pnpm store — nothing in
#   this tree sets `store-dir`, so the container, which has no home-directory
#   store to use, puts one beside the workspace. It is the registry copy its
#   `node_modules` hardlinks against, and it is root-written, so a host-side
#   delete would only ever half-succeed and report a refusal that never
#   resolves. Both facts point the same way: keep it.
#
#   `.nook-secrets/` is the case that proves `exclude` and `reclaim` answer
#   different questions. It is excluded from SEEDING because a credential
#   should not be copied into every card's worktree, and it must never be
#   reclaimed — it is not regenerable, and deleting it costs the fleet its
#   Claude session.
#
# Nothing is listed here on the strength of being gitignored alone. The `dist/`
# and tauri `binaries/` directories pass that test and still measure 0 bytes in
# every worktree on this node, so they stay out until they cost something.
#
# These overlap `exclude` above because both name the same class from opposite
# ends — what a fresh tree must not be GIVEN, and what a concluded one must not
# KEEP — and neither list is derivable from the other.
reclaim = [
  "/target",
  "/target-docker",
  "/target-docker-node",
  "/.cache/cargo-target",
  "/.cache/cargo-target-node",
  "frontend/node_modules",
  "frontend/packages/api/node_modules",
  "frontend/packages/app/node_modules",
  "frontend/packages/ui/node_modules",
  "frontend/apps/desktop/node_modules",
  "frontend/apps/web/node_modules",
  "frontend/apps/desktop/src-tauri/target",
]
