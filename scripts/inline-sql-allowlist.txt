# Files in crates/nook-control that hold inline SQL, and why (MAIN-260).
#
# Every remaining entry is a PERMANENT exemption (MAIN-292 AC-3/AC-4): a file
# that legitimately holds SQL because no aggregate owns what it touches. There
# are no "not migrated yet" entries left — the last of those went with MAIN-292,
# which is what closes the repo epic's AC-1. `scripts/check-inline-sql.sh`
# refuses NEW SQL in any file NOT listed here.
#
# PER FILE, never per directory. A directory exemption would let an already
# migrated file regress silently because a sibling is still pending.
#
# Adding a line back is not a routine step. If a new file needs SQL, the first
# question is which aggregate owns the table — the answer is a repository
# method, not an entry here. The guard also fails on a STALE entry, so a file
# listed below that stops holding SQL must lose its line in the same change.

# ── Mechanism, not an aggregate ─────────────────────────────────────────────
# These touch the database as plumbing rather than as data anyone models. There
# is no aggregate to move them to, so they stay where the mechanism lives.
crates/nook-control/src/routes/health.rs         # the liveness probe's SELECT 1: it proves the pool answers, so routing it through a repository would test the repository instead
crates/nook-control/src/ws/bus.rs                # bus_outbox + pg_notify: cross-instance fan-out, explicitly out of the epic's scope as mechanism (re-filed by MAIN-253)

# ── Permanently out of scope ────────────────────────────────────────────────
# Named by MAIN-260 NG-1 as legitimately holding SQL. Listed rather than
# hard-coded into the script so the exemption is visible where the others are.
crates/nook-control/src/seed.rs                  # bootstrap data; not an aggregate (NG-1)
crates/nook-control/src/main.rs                  # boot: migration + restamp wiring (NG-1)
