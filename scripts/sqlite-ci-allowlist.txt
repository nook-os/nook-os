# Tests that do NOT yet pass on SQLite (MAIN-270, epic AC-6).
#
# This file is the SQLite leg's remaining work, in one place. Everything NOT
# named here is COVERED: it passes on SQLite today and a failure fails the
# build. scripts/check-sqlite-ci.sh enforces both directions — a covered test
# that breaks reddens CI, and a test listed here that starts PASSING also
# reddens CI, so the list can only ever shrink.
#
# WHAT A LINE NAMES CHANGED ON 2026-08-25 (MAIN-657). It used to be a test
# BINARY, which while nook-control had 171 of them was the same thing as a
# file. That crate builds ONE binary now (`it`), so a line naming it would
# excuse 1160 tests to excuse ten. A line therefore names a SCOPE at whatever
# grain is honest — a single test (`it::job_reaper::a_stalled_job_is_reaped`),
# the module it lives in (`it::multi_instance`), or a whole binary
# (`nook_db:lib`) — and covers that identity plus everything under `::`. The
# binary form is exactly what every line used to be, so nothing that was
# already here changed meaning.
#
# THE CONVERSION MADE THE LEG STRICTER, not looser: the three files below each
# had tests that ALREADY PASSED on SQLite, and excluding the file excused those
# too. They are covered now, and a regression in one reddens CI.
#
# WHEN YOUR CARD LANDS, DELETE ITS LINES. That deletion is what grows the leg's
# coverage; the guard will tell you if you forget, and will not let you add a
# line for a failure that is already covered.
#
# EVERY LINE NAMES THE CARD THAT OWNS IT, because a list of failures with no
# owner is where failures go to be forgotten. Tests that are Postgres-only BY
# DESIGN have no card and never get deleted, so they are not in this file at
# all: see scripts/sqlite-ci-engine-specific.txt.
#
# WHAT THE SELF-TEST ACTUALLY CHECKS, and the gap that leaves (MAIN-423). It
# greps each entry for `MAIN-NN` — a SPELLING rule, not an ownership one. The
# guard proper never sees the tag at all: `strip()` deletes everything after
# `#` before parsing. So a line can name a card that is closed, or one that
# never existed, and CI stays green while the work it describes has no owner
# left to delete it. That is not hypothetical: 16 of these lines named
# MAIN-289 for weeks after that card went Done — its four children landed,
# nothing carried the remainder, and no check could notice. Re-tagged to
# MAIN-423 on 2026-08-05.
#
# IT THEN HAPPENED AGAIN, to the same lines, and the re-tag is why. MAIN-423's
# successor tag was MAIN-268, which closed on 2026-08-05 — so nineteen lines
# spent six days naming a third dead card while the conversion they described
# had already landed and the tests were failing on something else entirely.
# MAIN-478 re-measured them on 2026-08-11. A tag is a claim about a LIVE card
# and about a cause; moving one without re-running the binary carries both
# claims forward unchecked, which is how a wrong reason survives three cards.
#
# So when you CLOSE a card, grep this file for its number before you do.
# Nothing else will.
#
# Measured on two full runs of `cargo test --workspace --no-fail-fast` under
# sqlite://, which produced identical failure sets — nothing here is flaky.
#
# 2369 COVERED tests, 10 entries pending below, 2 deliberately Postgres-only —
# the numbers check-sqlite-ci.sh itself reports, not arithmetic on this file.
# (Read off the leg's own verdict by MAIN-657, which changed the UNIT: the line
# it replaces was `169 test binaries: 164 COVERED, 3 pending, 2 Postgres-only`.
# The two are not comparable and no arithmetic connects them — 164 covered
# binaries and 2369 covered tests are the same suite counted differently, and
# the leg genuinely covers more than it did, because the 11 passing tests inside
# the three suites below are no longer excused along with their file.)
# (Read off the leg's own verdict by MAIN-548, which deleted
# `dispatch_ownership`. It said 168/162/4/2 before, and arithmetic on that
# line would have given 168/163/3/2 — wrong on both totals, because a
# binary arrived while this branch was open and came in COVERED.)
# (Read off the leg's own verdict by MAIN-549, which deleted `squash_restamp`
# and moved `multi_instance` to the engine-specific file — so this is the first
# line where the third number is not 1.
#
# The line it replaces was `168/155/12/1`, and that one was WRONG well before
# this card touched it: #447, #448, #450 and #451 landed without re-measuring,
# so the header claimed 12 pending while the file below it held 6. Nothing
# detects that — the guard reads the entries and never reads this line — which
# is exactly why it says to run the gate and copy its sentence. Arithmetic on
# the stale line would have given 10 pending; the real answer is 4.
#
# Measured four times on this branch, once per rebase (#443 → #444 → #448 →
# #451), because a tally is a property of the tree it was read on and nothing
# carries across a rebase that changes the entry set. The last two runs agreed
# — #450 and #451 changed no binary's verdict — and it was still re-run, since
# "the numbers did not move" is a result, not an assumption.)
# (Read off the leg's own verdict by MAIN-545, which deleted ten lines. It said
# 166/143/22 before, and adding ten to that would have given 166/153/12 — right
# only about the pending count, because two more binaries arrived while this
# branch was open and both came in COVERED. Deleting a line is the one number
# you CAN predict; the other two have to be measured.)
# (Re-read from a real run by MAIN-385, which added `build_loop_pin`. It said
# 163/141/21 before, and this branch still carried the pre-#442 line — 153/132/20
# — until the rebase took main's. Two binaries arrived and one of them is
# covered, which is why arithmetic on the old line would have been wrong in both
# directions.)
# (Read off the leg's own verdict by MAIN-552, which added
# `build_port_leases_placement`. It said 153/132/20 before, and arithmetic on
# that line would have given 154/132/21 — wrong on both totals again, because
# nine binaries had become COVERED since it was last measured and nothing said
# so. Do not add one to what is here; run the gate and copy its sentence.)
# (Re-read from a real run by MAIN-478, which re-diagnosed every pending line
# and deleted none: nothing here passes yet. It said 147/126/20 before.)
# (Re-read from a real run by MAIN-509, which added `dispatch_order`. It said
# 141/121/19 before, and arithmetic on that line would have given 142/121/20 —
# wrong on both totals, because five binaries had become COVERED since it was
# last measured and nothing said so. Taken from the leg's own verdict on this
# PR instead: `✓ SQLite leg green over 126 covered binaries (20 excluded
# pending a card, 1 deliberately Postgres-only)`. Read it off the run; do not
# add one to what is here.)
# (Re-read from a real run by MAIN-472, which deleted `node_owner`; it said
# 137/116/20 before that.)
# (Re-read from a real run by MAIN-442, which deleted `task_workspace`. The line
# below said 103/61/41 while the file held 21 entries: cards deleted their lines
# and left this count behind, which is what a hand-maintained tally does. It is
# a note, not a check — the guard's own output is the number that matters.)
# (93/50/42 before MAIN-355. The suite gained binaries and MAIN-288's
# conversions landed; MAIN-355 itself added no line and removed none — it made
# `claim_lease` pass rather than excusing it.)
# (44/45 of 90 when this list was written; the suite has gained binaries since,
# and MAIN-293 then MAIN-294 deleted three lines between them.)
# (AC-1's "~55 of 101" was an earlier estimate of mine that counted the 11
# `Doc-tests` result lines as binaries; cargo prints 101 `test result:` lines
# but only 90 of them are test binaries. Corrected here against the real runs.)

# ── RE-DIAGNOSED 2026-08-11 (MAIN-478). None of these was what its line said ──
#
# Eighteen lines here read "raw sqlx / bed.pool in the test itself (MAIN-268)"
# and one read "with executor_selection (MAIN-268)". Every one of the nineteen
# named a card that CLOSED on 2026-08-05, and seventeen of them named a defect
# that no longer existed: the de-sqlx chain moved these tests off `bed.pool`
# onto `bed.db()`, which changed the RECEIVER and left the SQL alone, so the
# tests kept failing for causes their annotations never described. That is the
# gap the header warns about, arriving a second time — MAIN-289 → MAIN-423 →
# MAIN-268, three tags, one unexamined inheritance.
#
# Re-measured line by line on one `cargo test --workspace --no-fail-fast` under
# sqlite://, main at eb07629. NOTHING passed, so nothing is deleted; every line
# below now names the card that owns the failure the run actually produced.
#
# FIVE causes, five owners, and only ONE of them is the cause the old lines
# claimed (MAIN-549 has since been answered — see its entry):
#
#   MAIN-545  the TEST's own fixtures emit Postgres-only SQL — `interval '…'`,
#             `now()`, `gen_random_uuid()`, `::` casts, `CREATE SCHEMA`, and
#             `UPDATE <table> <alias>` without the `AS` SQLite requires (the
#             class MAIN-472 already fixed once, in node_owner).
#             LANDED 2026-08-12. Ten of its thirteen went green and their lines
#             are deleted. The other three each turned out to have a SECOND
#             cause behind the fixture, which only converting the fixture could
#             reveal — re-measured and re-tagged in place below, and the one
#             with no owner (`job_reaper`) got MAIN-564 filed for it. That is
#             the header's warning working as intended for once: a tag moved
#             only after the binary was re-run.
#   MAIN-546  PRODUCTION: `eligible_loop_executors` reads `json_each`'s alias
#             as a column.
#             LANDED 2026-08-13, and it took SIX lines with it, not the two
#             this block named. Four were the placement binaries that had
#             accumulated against the one query — executor_selection,
#             dispatch_order, workspace_build_loop_status (MAIN-495),
#             build_port_leases_placement (MAIN-552), build_loop_pin
#             (MAIN-385) — and the sixth was `loops_switch`, which MAIN-545
#             re-measured onto this cause the day before. The card's own text
#             named two because the other four arrived after it was written;
#             the deletions are what the guard checks, not the count in an AC.
#             A second defect in the SAME query had to be fixed to get there:
#             the `loop_kinds` test bound its needle JSON-encoded and cast it
#             to `jsonb`, and both halves are Postgres-shaped — SQLite reads
#             `CAST('"spec"' AS jsonb)` with NUMERIC affinity as the integer 0.
#             Fixing only the alias left 21 of executor_selection's 29 failing.
#   MAIN-547  PRODUCTION: `!= ALL($n)` bound an EMPTY list matches nothing on
#             SQLite where Postgres matches everything — and returns Ok, so an
#             unmounted node's checkouts are silently left un-tombstoned.
#   MAIN-548  the one line where the old reason was TRUE: still raw sqlx.
#             LANDED 2026-08-13, and it needed only what its line said:
#             the fixture INSERT moved to `Db::exec` + `params!`, which
#             binds `resources` as JSON on either engine. Nothing behind
#             the fixture — `owned_with_resources` was already
#             engine-agnostic — so this is the one of the five that had
#             no second cause waiting under it.
#   MAIN-549  the mechanism under test does not exist on SQLite; needs a ruling
#             before it needs code. RULED 2026-08-12, and the two binaries it
#             was filed for got DIFFERENT answers: `multi_instance` moved to
#             sqlite-ci-engine-specific.txt — SQLite is single-instance by
#             design, so a cross-instance bus is a Postgres CAPABILITY and not a
#             gap — and `squash_restamp` was FIXED, its failure being a
#             hardcoded Postgres MIGRATOR rather than a missing mechanism, so
#             its line is deleted rather than moved. Both are gone from below.
#             A THIRD binary briefly wore this number and does NOT belong to
#             that ruling. MAIN-545 re-measured `migrate_dev_tolerance` onto
#             MAIN-549 on 2026-08-12, after this card's acceptance criteria were
#             written and fixed at two binaries — so closing MAIN-549 would have
#             left a pending line naming a done card, the exact state the header
#             above warns about three times. It is re-tagged to **MAIN-566**,
#             filed for it: its `VersionMissing(38)` half is the same
#             hardcoded-migrator defect and takes the same fix as
#             squash_restamp, while its empty orphan list is `orphan_versions`
#             answering honestly on SQLite and needs a gate, not a ruling.
#             Neither half needed the ruling this card was raised for, which is
#             why it is a separate owner rather than a third bullet here.
# ── job_reaper: 6 of its 13, re-run for the conversion 2026-08-25 ──
#
# The MEASURED line below says `3 passed, 6 failed` and is kept as written. The
# file held nine tests when it was measured and holds thirteen now: six still
# fail — the six named under it — and the four that arrived since came in
# COVERED, which is precisely what naming tests instead of a file buys.
# MEASURED (MAIN-545, 2026-08-12, after its three fixtures were converted): `near "j": syntax error` — production `repo/jobs.rs:1367` is `UPDATE loop_jobs j … FROM nodes n`, and its `RETURNING n.last_seen_at` names the joined table SQLite cannot return from. 3 passed, 6 failed (MAIN-564)
it::job_reaper::a_claimed_or_running_job_on_a_dead_executor_is_reaped  # (MAIN-564)
it::job_reaper::a_job_whose_executor_was_seen_recently_is_untouched  # (MAIN-564)
it::job_reaper::a_paused_job_is_never_reaped  # (MAIN-564)
it::job_reaper::a_reaped_job_can_be_re_run  # (MAIN-564)
it::job_reaper::a_run_orphaned_by_an_agent_restart_does_not_stay_running_forever  # (MAIN-564)
it::job_reaper::the_reap_is_atomic_and_loses_a_live_transition_cleanly  # (MAIN-564)

# ── migrate_dev_tolerance: 3 of its 6 ──
# MEASURED (MAIN-545, 2026-08-12, after the bytea literals were bound and the two schema/psql tests gated; re-measured unchanged by MAIN-549, 2026-08-13): `VersionMissing(38)` and an empty orphan list — the Postgres MIGRATOR driven at a SQLite bed, and `orphan_versions` returning empty there BY DESIGN. It needs the same FIX as squash_restamp, not the same ruling. 3 passed, 3 failed (MAIN-566)
it::migrate_dev_tolerance::dev_boot_proceeds_past_an_orphan_row  # (MAIN-566)
it::migrate_dev_tolerance::dev_tolerance_does_not_mask_a_modified_migration  # (MAIN-566)
it::migrate_dev_tolerance::production_stays_fatal_on_an_orphan_row  # (MAIN-566)

# ── reconcile_tombstones: 1 of its 5 ──
# MEASURED (MAIN-547, 2026-08-13): the production `path != ALL($2)` half is FIXED and its three assertions pass; the one remaining failure is the fixture `now() - ($8::bigint * interval '1 second')` at :246. That cause is MAIN-545's class but MAIN-545 NG-2 disowns this binary, so the fixture half has no card yet — this line waits on one (MAIN-547)
it::reconcile_tombstones::retention_sweep_removes_only_expired_rows  # (MAIN-547)

# ── the MAIN-423 residue, and what MEASURING it found (MAIN-472) ──
#
# MAIN-423 closed as a tracker on 2026-08-08 and its one surviving line —
# node_owner — moved here. It was measured rather than inherited, and the reason
# it had carried since MAIN-289 was wrong: nothing on that path emits
# Postgres-only SQL from production. Its single remaining failure was
# `UPDATE nodes n SET …` in the TEST itself, which SQLite refuses for want of
# the `AS` that Postgres treats as optional. Fixed there, so the line is DELETED
# and no entry names MAIN-423 any more.

# ── still production Postgres SQL, re-tagged by MAIN-293 ──
#
# These carried a MAIN-293 tag because that card's evidence was gathered with a
# pattern matching BOTH `NOT NULL constraint failed: users.person_id` and
# `no such function: gen_random_uuid` — two different defects. MAIN-293 fixed
# the first (the missing column default) and every one of those failures is
# gone; what remains here is the second, which is production emitting Postgres
# SQL and belongs to the dialect sweep. `local_accounts` passed outright and its
# line is deleted.
