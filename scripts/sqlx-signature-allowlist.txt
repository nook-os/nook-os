# Files outside crates/nook-db that still name a sqlx TYPE, and why (MAIN-268).
#
# Generated from the tree as it stood when the guard landed, so the guard is
# green on its first run. Every entry is a file whose card has not landed yet,
# or one that will never convert — not a permission to add more.
# `scripts/check-sqlx-signatures.sh` refuses a sqlx type in any file NOT listed
# here, and refuses a listed file that no longer has one, so the list can only
# shrink.
#
# PER FILE, never per directory. A directory exemption would let an already
# converted file regress because a sibling has not been done yet.
#
# nook-db is absent because it is the ADAPTER: sqlx is its whole job, and the
# guard does not scan it at all.

# MAIN-269 (#218) freed seven files that named sqlx::Error only because
# nook-db's Db trait returned it. They are gone from this list, which is the
# list doing its job: the stale-entry check named all seven the moment #218
# landed, and refused to pass until they were removed.

# MAIN-327 (this card) freed the two files that were listed here as PERMANENT:
# `repo/notifications.rs` and `nook-types/src/lib.rs`, both of which named
# `PgRow`/`SqliteRow` in hand-written `FromRow` impls. The note said replacing
# them "is replacing row mapping, and needs its own decision" — that decision is
# what MAIN-327 made: row mapping is now `nook_db::FromDbRow` over an
# engine-neutral `DbRow`, so those impls do not exist to exempt. Nothing in this
# list is permanent for the reason "sqlx owns row mapping" any more.

# ── Permanent: the thing under test IS a Postgres feature ───────────────────
crates/nook-chat/src/main.rs                 # PgConnectOptions — chat pins search_path=chat,public; nook_db::connect has no option for it
crates/nook-chat/tests/auth_search_path.rs   # the subject IS the search_path pin
crates/nook-chat/tests/migrations.rs         # its own sqlx::migrate::Migrator into the chat schema
crates/nook-control/tests/sqlite_person_id_default.rs  # sqlx::Row to read pragma_table_info on a raw SQLite handle

# ── Awaiting MAIN-268's own AC-1 (the bed.pool removal) ─────────────────────

# ── Awaiting the testkit conversion (MAIN-267 and its siblings) ─────────────
# Each line goes when its file is converted.
crates/nook-control/tests/oidc_degraded.rs   # sqlx::raw_sql runs the real 2-statement migration file verbatim; `Db` has no multi-statement path (MAIN-393), so it opens its own pool from bed.database_url()

